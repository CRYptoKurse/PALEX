fn add(int a, float b) -> float {
    float sum = a + b;
    return sum;
}

fn main() -> int {
    int x = 1;
    float y = 2.5;
    float z = add(x, y);
    bool cond = true;
    if (cond) {
        x = x + 1;
    }
    return x;
}
